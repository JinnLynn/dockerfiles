FROM jinnlynn/alpine:armhf

MAINTAINER JinnLynn <eatfishlin@gmail.com>

RUN set -ex && \
    apk add --no-cache aria2

VOLUME /app/run

EXPOSE 6800

CMD [ ! -f /app/run/aria2.session ] && touch /app/run/aria2.session || true && \
    aria2c --conf-path=/app/etc/aria2.conf
