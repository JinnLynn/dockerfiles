server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
        default_type text/html;
        return 200 "It's work!";
    }
}
